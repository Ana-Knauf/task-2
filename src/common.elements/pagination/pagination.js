$("#pagination-demo").pagination({
    // options here
    pageIndex: 0,
    pageSize: 12,
    total: 200,
    showInfo: true,
    pageElementSort: ['$page','$size','$jump','$info'],
    pageBtnCount: 6,
  });
